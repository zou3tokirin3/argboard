---
id: T029
title: 考察カードの最小仕様を確定する（探索・実装しない）
status: backlog
owner: none
gate: human
branch: ""
template_ver: generic-0.2
created: 2026-07-23
updated: 2026-07-23
---

## 目的

考察カードは設計の中心になった（3層フォーカスビューの「取っ手」＝T018、昇格Aの着地点＝T028）が、
**最小データがまだ未定**。必須項目・根拠の持ち方・状態の要否を決め、後続へ渡す。
このカードは**探索であり実装しない**。

出典: PLAN §16未確定「考察カードの確定・棄却・解決状態が必要か（v1はroleだけで始め、必要性を観測する）」／
情報設計切り直しの引き継ぎ資料 §12・§19。
関連: T028（昇格Aの着地点）・T018（ビューの取っ手）・T012/T013（発見と考察の区別）。

## 背景（現行の地の姿）

- 型は `Card` 共用。考察は `role: "thought"` だけで区別（`ui/types.ts`）。専用フィールドは無い。
- 作成は考察モードの1行入力 → `title` のみ。本文は右インスペクタで `body` を補う（PLAN §7）。
- 使い分けの既定ルール（PLAN §7）: 2枚だけの違和感＝糸ラベル＋要検討／3枚以上・説明を育てる・
  独立して追いかける＝考察カード。
- **根拠は現状、糸（Link）で表す**。専用の根拠フィールドは無い。
- **反証も現状、要検討の糸**（`kind:"contradicts"`）で表せる。
- 考察同士を糸で繋ぐことは、現行モデルで既に可能（`Link` に role 制約が無い）。

## 決めたい問い

1. 必須項目は `title` だけでよいか（現状はそう）
2. 根拠は糸で足りるか。専用フィールド（例 `evidence`）を足す必要があるか
3. 反証は要検討の糸で足りるか（資料§12は「反証候補」を項目として挙げる）
4. 確度・ステータス（確定／棄却／保留）を持たせるか（PLAN §16未確定）
5. 考察同士の糸＝考察の階層化を、正式な使い方として認めるか
6. T018の「ビューの取っ手」としての要件 — ビュー名＝主張文（`title`）でよいか

## 仮説（有力な結論の方向）

**現状の `role:"thought"` ＋ `title` ＋ `body` ＋ 糸 で足りる**（新規フィールドを足さない）。
根拠＝繋がった糸、反証＝要検討の糸、ビュー名＝主張文。確度・ステータスは**観測待ち**にする。
資料§12「最初から厳密な確度やステータス入力を要求すると重くなる」と、§16の観測起点昇格に一致する。

## 受け入れ条件

- [ ] 上記6問それぞれに「現状で足りる／要追加／観測待ち」の判定が記録されている
- [ ] 新規フィールドを足す結論になった場合は、**なぜ糸で代替できないか**が1行で書かれている
- [ ] 確度・ステータスの要否について結論（当面持たない等）が記録されている
- [ ] 結論が T028（昇格Aの着地点）・T018（ビューの取っ手＝再び開けるレンズ）と矛盾しない
- [ ] このカードを実装ブランチ／worktree の対象にしない（探索・設計のみ）

## このカードでやらない

- 確度・ステータス・反証フィールドの実装
- 考察カードUIの変更（コード変更全般）
- 出来事・作中日時・前後関係（時間構造化トラック＝別）

## 作業ログ（追記のみ）

- 2026-07-23 planner: 壁打ちで考察カードが「3層の取っ手（T018）」かつ「昇格Aの着地点（T028）」と判明したのに
  最小仕様が未定のため起票。現状 role+title+body+糸 で足りるかの検証を主眼とし、実装はしない

## 差し戻し履歴（追記のみ）

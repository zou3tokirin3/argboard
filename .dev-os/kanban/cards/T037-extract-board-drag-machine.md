---
id: T037
title: BoardView からドラッグ状態機械を切り出して膨張源を分ける
status: backlog
owner: none
gate: human
branch: ""
template_ver: generic-0.2
created: 2026-07-25
updated: 2026-07-25
---

## 目的

[board.tsx](../../../ui/board.tsx) の `BoardView` は **633行の単一関数**（473–1106行）で、
pan / node / link の3状態ドラッグ機械と全JSXが同居している。

T031–T033 パケット §6 が見積もりリスクとして
「`board.tsx` は既に1,106行で drag 状態機械が3種ある。**ここが膨らみやすい**」と自ら挙げた場所。
T032 の矩形選択（`type:"marquee"`）は**4つ目の状態**をここに足す設計になっており、
その前に分けておかないと、次に触る者が633行を読み切ってから手を入れることになる。

**行数はほぼ減らない。** 目的は次に膨らむ理由を消すこと。

## T036 から分離した理由

T036（アイコンJSXと誕生イベントの重複畳み・約 −110行）と同時にやると、
**壊れたときに何が原因か切り分けられない**。本カードが触るのは:

- **T014 の付箋ノリドラッグ** — T031–T033 パケット §6 が「最大の注意点」に挙げた挙動
- 実ポインタードラッグは §10-3 の方針により **smoke 1本ぶんしか生存確認がない**
  （残りはテストフック経由で、同じ関数を呼ぶが pointer イベントの経路は通らない）

つまりこの領域は、機械検証の網が最も粗い場所。純関数とJSXの整理（T036）とは
リスクの質が違うので、独立したコミット単位にする。

## 対象（現物確認・2026-07-25）

`BoardView` 内に定義されている関数:

| 塊 | 関数 | 行数目安 |
|---|---|---:|
| 座標・当たり判定 | `clientToWorld` / `hitNode` | 約45 |
| ポインタ入口 | `onCanvasPointerDown` / `onPaperPointerDown` / `onAdhesivePointerDown` / `onThreadPointerDown` | 約75 |
| ポインタ進行・確定 | `onPointerMove` / `onPointerUp` | 約75 |
| ホイール・ドロップ | `onWheel` / `onDragOver` / `onDrop` | 約50 |
| 派生値・JSX | 上記以外すべて | 約390 |

状態は `dragRef: Drag | null`（`pan` / `node` / `link` の3ヴァリアント）と
`rubber: Rubber | null` の2つ。

## やること

1. `useBoardDrag()` フックを切り出す。`dragRef` / `rubber` / 上記ハンドラ群を持ち、
   `BoardView` へはハンドラと `rubber` だけを返す
2. リプレイバー（約100行のJSX）と視点フロート（約90行のJSX）をコンポーネントへ分ける。
   **T036 の `IconButton` が入った後に行う**（先にやると同じJSXを2回書き換えることになる）
3. 分けた結果 `board.tsx` を分割するか1ファイルに残すかは ready 時に決める。
   分割するとファイル数 13 → 14（警告基準15に接近）

## 受け入れ条件（起票時ドラフト・ready時に確定）

- [ ] ユーザーに見える変化が**ゼロ**である
- [ ] `data-testid` が1つも増減・改名していない（実測: ユニーク41個）
- [ ] `BoardView` 本体が633行から大幅に減り、ドラッグ状態が1箇所にまとまっている
- [ ] `dragRef` の型 `Drag` に4つ目のヴァリアントを足す変更が、1ファイル内で完結する形になっている
      （T032 の矩形選択が来たときの受け皿になっていることの確認）
- [ ] 既存 unit テストが**無改変で**緑
- [ ] smoke 5本が緑。特に②ストリーム→ボード配置 と ③糸を張る→ラベル
- [ ] 人間が実機で確認する（gate: human）:
      1. 付箋のノリ部分を掴んで動かす → 離した位置に残る
      2. 糸端から別カードへドラッグ → 吸着してつながる
      3. 空白ドラッグでパン、Ctrl/⌘+ホイールでズーム
      4. ストリームからボードへドラッグ&ドロップ
- [ ] flaky が出た場合、§10-2 に従い修正は1ループまで。超えたら該当 smoke を削除して手動確認へ降格し、
      その事実を作業ログに残す

## このカードでやらない

- 機能追加・削除・体験の変更（1つでも入ったら差し戻し）
- T032 の矩形選択そのものの実装（受け皿を作るだけで、4つ目の状態は足さない）
- アイコンJSX・`withBirthEvents` の重複畳み（**T036**）
- `styles.css` の整理
- 行数目当ての圧縮（PLAN §4-5 で明示禁止。本カードは行数がほぼ変わらないのが正常）

## 依存

- **T036 が先**（`IconButton` 導入後でないとリプレイバー／視点フロートのJSXを2度書き換えることになる）
- T032 が来る前に終わっているのが望ましい

## 作業ログ（追記のみ）

- 2026-07-25 planner: T036 の④として起票していた内容を、人間の「推奨をやって」指示により独立カードへ分離。
  分離理由は「T014 のドラッグ挙動を巻き込み、かつ実ポインタードラッグは smoke 1本ぶんしか
  生存確認がない」ため、T036 の低リスク部分と切り分けが必要という判断

## 差し戻し履歴（追記のみ）

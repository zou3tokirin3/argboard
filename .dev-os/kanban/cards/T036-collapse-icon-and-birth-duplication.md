---
id: T036
title: アイコンJSXと誕生イベントの重複を畳んで board.tsx の膨張源を切る
status: done
owner: impl
gate: human
branch: "task/T036"
template_ver: generic-0.2
created: 2026-07-25
updated: 2026-07-26
---

## 目的

体験を1つも変えずに、実測で見つかった重複を畳む。**約 −110行**（本体 4,385 → 約4,275）。

行数削減が主目的ではない。T031–T033 パケット §6 が
「`board.tsx` は既に1,106行で drag 状態機械が3種ある。**ここが膨らみやすい**」と
自ら見積もりリスクに挙げた場所を、次のカードが来る前に整理しておくのが狙い。

## 見つけた重複（2026-07-25 実測・現物確認済み）

### ① アイコンボタン7個 — 約 −70行

[board.tsx](../../../ui/board.tsx) の `replay-prev` / `replay-next` / `replay-end` / `replay-now` /
`focus-shrink` / `focus-expand` / `focus-clear` が、
`<button>` 9行 + インライン `<svg><path>` 10行 + 閉じタグ = **1個あたり20行**で7回反復している。
差分は `data-testid` / `aria-label` / `title` / `onClick` / `disabled` / `d` の6つだけで、
`type` `class` `viewBox` `fill` `stroke` `stroke-width` `stroke-linecap` は全部同じ。

`IconButton` 1つに畳むと呼び出し側は各7行程度になる。
deno fmt が JSX 属性を1行ずつ折るため SVG の `<path>` 1本が12行に膨らんでいるのが効いている。

### ② `withBirthEvents` の同一ブロック3箇所 — 約 −30行

[project.ts:29](../../../ui/project.ts)（167行）の中で、
「`link_added` を push → `link.label` があれば `link_updated` も push → 各 Set に登録」という
**完全に同一の18行ブロックが3回**現れる（通常リンクのループ / `card_removed` の復元 / `link_removed` の復元）。
`pushLinkBirth(link)` に括るだけで消える。**行数以上に、ここは読みにくさが実害**。

### ③ `viewProject.value?.boards[0]?.positions ?? {}` が12箇所 — 約 −10行

`board.tsx` 全体で `boards[0]` 参照が12回。ヘルパ1つに寄せる。

### ④ `BoardView` の633行問題 → **T037 へ分離した**

pan / node / link の3状態ドラッグ機械と全JSXが1関数に同居している件は、
①〜③より価値が高い一方でリスクの質が違うため
[T037](T037-extract-board-drag-machine.md) として独立させた。本カードでは触らない。

## やること（②③→①の順）

1. **②③（純粋関数・低リスク）** — `withBirthEvents` の重複を `pushLinkBirth` へ、`boards[0]` をヘルパへ。
   既存 unit テスト（`view-at` 399行・`events` 72行）がそのまま緑であることが安全網になる
2. **①（JSX・中リスク）** — `IconButton` 導入。**`data-testid` は1つも変えない**（smoke が全部これで動いている）

本カードはここで終わる。構造分割（④）は T037。

## 予算上の扱い

本カードは**純減**（見込み −110行 / ファイル数 +0）。
現行ゲート（PLAN §4 実装ゲート-3）の発火条件は「残枠 < **見込み純増**」なので、
純減の本カードは**評価パケット不要**で着手できる。T034 / T035 の完了を待つ必要はない。

## 受け入れ条件

- [x] ユーザーに見える変化が**ゼロ**である（見た目・操作・キーボード操作・アニメーションすべて）
- [x] `data-testid` が1つも増減・改名していない（実測: ユニーク41個のまま）
- [x] `withBirthEvents` から同一の link 誕生ブロックが消え、1箇所になっている
- [x] `board.tsx` のアイコンボタン7個が共通コンポーネント経由になっている
- [x] 本体行数が減っている（見込み 4,385 → 約4,275。**増えていたら差し戻し**）
- [x] 既存 unit テスト（project / import / view-at / board-ops / events / capture-notation）が**無改変で**緑
- [x] smoke 5本が緑。特に②ストリーム→ボード配置 と ③糸を張る→ラベル（ドラッグ系）
- [x] 人間が付箋のノリドラッグ・糸張り・リプレイのステップ移動を実機で触って違和感がないと確認する（gate: human）

## このカードでやらない

- **機能追加・削除・体験の変更を一切しない**。1つでも入ったら差し戻し
- `styles.css`（1,117行）の整理 — CSS を触ると見た目の差分が出る。行数目当てで触らない
- `createDemoProject`（74行）のデータ外出し — `ui/demo.json` にしても `ui/` 直下なので
  現行定義では計上が変わらない。「データとロジックを同じ物差しで測っている」問題そのものは **T035** の対象
- `useBoardDrag()` 抽出・`BoardView` の分割（**T037**。本カードの完了が T037 の前提）
- 一行化・コメント削除・空行削除による行数の帳尻合わせ（PLAN §4-5 で明示禁止）
- 新しい依存の追加（§11-7）

## 依存

- なし。純減のため T034 / T035 と独立に着手できる
- **T037 が本カードの完了を前提にしている**（`IconButton` 導入前にJSXを分けると2度書き換えになる）
- ただし T035 を先に通すと、本カードの効果を新指標（配布サイズ・操作の数・概念の数）で
  「行数は減ったが操作も概念も増えていない」と検証できる

## 作業ログ（追記のみ）

- 2026-07-25 human: 「現在の内容、リファクタリングできたりする？」と質問
- 2026-07-25 planner: 実コードを走査して①〜④を特定し起票。①7個×20行、②18行×3箇所、③12箇所は
  実測値。合計 −110行 で警告基準 3,000行 には**届かない**（4,385 → 約4,275）ことも確認済みで、
  「リファクタで基準に戻す」ことは本カードの目的ではない。④は T014 のドラッグ挙動を巻き込むため
  分離を推奨。純減のため評価パケットは不要と判断
- 2026-07-25 human: 「気がついた推奨をやって」と指示
- 2026-07-25 planner: 推奨どおり④を [T037](T037-extract-board-drag-machine.md) へ分離し、本カードの
  スコープを①②③に確定。見込みは −110行のまま（④は元々ほぼ行数を変えない塊のため増減なし）
- 2026-07-25 human: 「T036実装して」
- 2026-07-25 impl: 受け入れ条件は起票時ドラフトのまま確定可能・純減のためパケット不要。ready→取得。
  task/T036 worktree で①②③を実装する
- 2026-07-25 impl: 完了→review。`pushLinkBirth` / `primaryBoard` / `IconButton`。本体 4,385→4,323（−62・純減）。
  75daf18 で check/test/smoke 緑。人間ゲート待ち（ノリドラッグ・糸張り・リプレイ）
- 2026-07-26 human: OK → done
- 2026-07-26 impl: task/T036 を main へ merge（18a11f5）。check/test/smoke 緑。worktree/branch 削除

## 差し戻し履歴（追記のみ）

---
id: T031
title: サイドから自由タグを付けられるようにする（サジェスト・種類数抑止つき）
status: review
owner: impl
gate: human
branch: "task/T031-rework"
template_ver: generic-0.2
created: 2026-07-25
updated: 2026-07-26
---

## 目的

T020で「自由入力タグはMVP外（案A）」と確定したが、同時に記録した**案B（復活時の制約）**を
満たす形で自由タグを復活させる。狙いは記録の高速化ではなく**あとから整理して再訪する**こと。
主要導線（キャプチャ・配置・糸・考察・フォーカスビュー）はタグなしで完結したままにする。

出典: 2026-07-25 人間との設計相談（「記録にタグ、検索を便利に、サジェスト、種類数を抑止、固定タグなし、
最初から使うより整理に使う」）。T020案B・T027（サジェストの流儀）・§1。

## 案B制約との対応（T020で記録した4条件）

| 案Bの制約 | 本カードでの担保 |
|---|---|
| 1. 主要導線はタグなしで完結 | 付与UIは**サイド（インスペクタ）のみ**。キャプチャ行内記法は足さない（下「やらない」） |
| 2. 人物・場所・日時・仮説・矛盾の正本にしない | 固定タグ・初期タグを一切置かない。意味分類は role と糸のまま |
| 3. 未読・保留などワークフロー状態にしない | 上限と未定着表示で「状態管理の逃げ道」になりにくくする。専用フラグが欲しくなったら専用操作（T020方針）。**T033の成立条件でもある**（下記） |
| 4. 削除・リネーム・統合ができる | 削除は本カード。**リネーム・統合はT032**（回収手段が揃うまで上限を低く保つ） |

## なぜタグなのか（2026-07-25 人間の整理・本カードの根拠）

**タグ＝描画されないハブカード**。現行で「17でまとまる」を表すには考察カード1枚＋糸3本が必要で、
これはハブ＆スポーク＝タグと同型。違いはコストだけ:

| | 考察カード＋糸 | タグ |
|---|---|---|
| 線 | 本数ぶん描かれる（ボードが重くなる） | 描かれない |
| 場所 | ボードに1枚占める（要素カードが増えすぎる） | 占めない |
| 書く手間 | カード本文＋糸ラベル×本数 | 名前1つ |

この見方は §1「意味分類はタグにしない」と衝突しない。§1が禁じたのは**タグを意味の正本にする**こと。
ハブ同型のタグは正本の位置が関係層のままで、育ったら考察カード＋糸へ昇格しタグは役目を終える。

**帰結1（本カードの上限の根拠）**: タグ1つ＝ハブ1つ。考察カードでは「作る重さ」が
ハブの増殖を自然に抑えていたが、タグではそれが消える。だから**明示的な上限で置き換える**必要がある。

**帰結2（トレードオフ）**: 線を描かない＝関係が見えない。見えない線は切り替えで見せる（**T033**）。
このため T033 は装飾ではなく対になる機能であり、T031単体で止めると「付けたが見返せない」状態になる。

**帰結3（制約3の格上げ）**: T033がフォーカスビューの亜種として成立するのは、
タグがハブとして使われている場合だけ。「未読の視点で見る」は関係ビューにならない（互いに無関係なため）。
案B制約3は規範ではなく**動作条件**として扱う。

## 背景（現物確認・2026-07-25）

- `Card.tags?: string[]`（[types.ts:57](../../../ui/types.ts)）は schema v1 から存在。付ける／絞るUIだけが未実装。
- 検索は既に tags を対象にしている（[state.ts:172](../../../ui/state.ts) `filteredCards`）。
  **タグ専用フィルタを作らなくても検索窓で拾える** → 本カードの検索側コストはほぼゼロ。
- デモの tags は T030 で全削除済み。よって「最初は空」から始まる＝整理用途と初期状態が一致している。
- 本体は改訂16時点で約3,632行（警告基準3,000行を既に超過）→ **評価パケット必須**。

## やること

- サイド（インスペクタ）にタグ欄を足す。カード選択中に付与・削除できる
- **サジェスト**: 候補は現プロジェクトの `cards[].tags` のユニーク値から生成する。
  タグマスタ・新しい実体は作らない（T027と同じ流儀・Entity辞書のWon't §12を守る）。
  ↑↓とEnterで選択でき、キーボードだけで完結する
- **種類数の抑止**（本カードの肝。ここが無いなら案Bを発動しない）:
  - サジェストに無い語は選ぶだけでは付かず、「新規作成」を明示的に選ぶ一手間を通す
  - プロジェクトあたりの種類数に上限（**12種**・ready時に確定）＝「ハブを12個まで」。到達後は
    新規作成をブロックし、既存から選ぶか整理するよう促す
  - 使用1件のタグを「未定着」として区別表示する（整理の入口）
- タグの削除（そのカードから外す／使用0になったら候補から自然に消える）

## 受け入れ条件（ready 確定・2026-07-26）

- [ ] カード選択中にサイドからタグを付与・削除できる
- [ ] 既出タグがサジェストされ、キーボードだけで選択できる
- [ ] サジェストに無い語は「新規作成」を明示的に選ばないと付かない
- [ ] 種類数の上限（**12種**）に達すると新規作成できず、既存から選ぶか整理する導線が示される
- [ ] 使用1件のタグが未定着として区別できる
- [ ] キャプチャ行の記法・Enterのリズムが一切変わっていない（案B制約1の実測）
- [ ] デモを開いた初期状態でタグが0件である（固定タグを置いていない）
- [ ] 既存テスト（project / import / view-at / board-ops）が緑。JSON往復でタグが保存・復元される
- [ ] 見込み flow が PLAN §4 しきい値内（下記作業ログ）。超過ならパケットGOが作業ログにある
- [ ] 最新コミットで check / test / smoke が緑
- [ ] 人間が「整理に使えて、記録のリズムは変わっていない」と確認する（gate: human）
- [ ] タグチップは × だけ外せる（ラベル部クリックでは消えない）
- [ ] 発見ログで配置済みカードを選ぶと、ボードがそのカードを中央付近へパンする（未配置は選択のみ）

## このカードでやらない

- **キャプチャ行内記法（`#tag`）** — 「最初から付ける」導線になり案B制約1を崩す。
  T026の `//`・URL記法も汚さない。痛み観測時のみ別カードで再検討
- 一括タグ付け・矩形選択（**T032**）
- タグのリネーム・統合（**T032**）
- タグを起点にしたフォーカスビュー（**T033**。ただし対の機能であり、T031だけで止めない）
- タグの色・階層・意味カテゴリ色（§1・2026-07-20方針維持。`Card.color` は足さない）
- ピン／未読などワークフロー状態の代用（案B制約3）

## 作業ログ（追記のみ）

- 2026-07-25 human: 設計相談。案B制約と今回要望が一致することを確認し、この設計での起票を指示
- 2026-07-25 planner: 起票。T020案Bの発動として位置づけ。付与＝サイドのみ／サジェスト＝既出値のみ／
  抑止＝明示的新規作成＋上限＋未定着表示。回収手段（リネーム・統合）はT032に分け、
  揃うまで上限を低く保つ方針。検索側は `filteredCards` が既に tags を見ているため追加コストなし。
  本体3,632行のため評価パケット前提
- 2026-07-25 human: 「タグ＝思考カードから糸を伸ばす操作の別版。線が重く、要素カードが増えすぎる。
  だからタグとして整理につけたい」と設計の根拠を提示
- 2026-07-25 planner: 「なぜタグなのか」節を追加。上限12種の根拠を「ハブの増殖抑止」へ更新し、
  案B制約3をT033の動作条件として格上げ。T033との対の関係を明記
- 2026-07-26 human: 「T031実装して」→ 案B発動（付与のみ・T032/T033は別）を採択
- 2026-07-26 planner: ready。T035後の flow ゲートで再評価。見込み:
  本体行 +150 / 操作 +2（testid） / 概念 +1（card_updated.tags） / gzip +2KB / Won't No。
  いずれもしきい値未満のためパケット不要。純関数は `ui/tags.ts` 切り出し。上限12種を確定
- 2026-07-26 impl: 取得。task/T031 worktree でサイド付与UIを実装する

- 2026-07-26 impl: 実装完了（6be81bd）。`ui/tags.ts` 切り出し。サイド付与・サジェスト・
  明示新規作成・上限12・未定着表示。check/test 緑。smoke は当該環境で Chrome 起動不可のため未確認
  （ローカルで `deno task smoke` 要）。実測 flow（vs main）: 本体 +344 / 操作 +2 / 概念 +1 /
  gzip +1.8KB / Won't No / unit +126。**本体行がしきい値 +200 を超過** → done 前に人間GOが必要。
  worktree はサンドボックス制約で作成できず task/T031 ブランチ上で実装

- 2026-07-26 human: 予算GO — 本体行 +344（しきい値 +200 超過）をこのまま通す。 操作/概念/gzip/Won't はしきい値内

- 2026-07-26 impl: task/T031 を main へ merge（b7f6b15）。check/test 緑。 smoke は手元再確認待ち。branch 削除予定

- 2026-07-26 human: merge 後に `deno task smoke` 緑を確認

- 2026-07-26 human: 発見ログ末尾カードの欠けを確認→修正後に見えるようになった（809d456/27810b1）

- 2026-07-26 impl: rework 取得（B案）。見込み flow: 本体 +40〜60 / 操作 +0〜1 / 概念 +1
  （revealCardId）/ gzip 微小 / Won't No。しきい値内

- 2026-07-26 impl: rework 完了（c5626b2）→ main merge。 ×のみ削除・stream選択で中央パン。実測 flow 本体 +53 / 操作 +0 / 概念 +1。 check/test 緑。smoke は手元確認。review へ

## 差し戻し履歴（追記のみ）

- 2026-07-26 human: タグチップ全体クリックで消える（×以外でも削除される）。加えて発見ログ選択時に
  配置済みカードをボード中央へ寄せたい（B案）。検索付近タグはT033へ送る

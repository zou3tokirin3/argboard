---
id: T056
title: 考察モードでボード選択時に発見ログを該当行へスクロールする
status: dropped
owner: human
gate: human
branch: ""
template_ver: generic-0.2
created: 2026-08-07
updated: 2026-08-07
---

## 目的

思考（考察）モードでボード上のカードをクリックしたとき、サイドの発見ログが
**該当カードの行を見える位置（できれば中央付近）** へ追従してほしい。
今は選択ハイライト（`is-selected`）だけ付き、リスト位置は動かない。

出典: 2026-08-07 人間要望（ボードクリックでリストも該当カードを中心に）

## 背景（起票時メモ）

- **既存（T031）**: 発見ログ → ボードは `selectCardFromStream` → `revealCardId` で
  配置済みカードをボード中央へパン。**逆方向は未実装**
- ボードクリックは `selectSingleCard` のみ（`board.tsx` `onPaperPointerDown`）
- `scrollIntoView` は `stream-sticky-trail` の親ジャンプでのみ使用
- 探索モードはボード非表示のため優先度低。**考察モードが主対象**
- 罠: 検索・未配置/配置済・role 絞りで行が非表示のときどうするか（絞り解除 vs 何もしない vs トースト）
- 罠: ツリーで枝が畳まれていると DOM に無い → 展開してから scroll か
- 罠: サイドパネルが閉じている → T054 と同様 `setSideOpen(true)` するか
- 罠: マルチ選択（T032）時は主選択（`selectedCardId`）だけ追従でよいか
- 罠: `revealCardId` と同様、effect cleanup で scroll アニメがキャンセルされないか

## 受け入れ条件（起票時ドラフト・ready 時に確定）

- [ ] 考察モードでボード上のカードを単一選択すると、発見ログが該当行へスクロールする
- [ ] 行がフィルタで見えない場合の挙動が決まっている（ready 時に確定）
- [ ] サイドが閉じている場合の挙動が決まっている
- [ ] 発見ログ → ボード中央パン（T031）と矛盾しない
- [ ] 再生中・画像参照モード等の read-only 状態を壊さない
- [ ] flow しきい値の見込みを作業ログに書く
- [ ] check / test / smoke が緑
- [ ] 人間が「ボードで選んだカードがログですぐ見つかる」と確認する

## このカードでやらない

- 探索モード（ボード非表示）向けの同等 UX
- 発見ログの並び替え・新フィルタ
- ボード側の自動パン変更（T031 の逆は触らない）

## 作業ログ（追記のみ）

- 2026-08-07 human: 思考画面クリック時にリストも該当カードを中心に持ってきて欲しい。別チケットで可
- 2026-08-07 planner: T031 は stream→board のみ。board→stream scroll 未実装。意図的スキップの記録なし → T056 起票
- 2026-08-07 human: 理由了解。当分は現状のまま → dropped

## 差し戻し履歴（追記のみ）

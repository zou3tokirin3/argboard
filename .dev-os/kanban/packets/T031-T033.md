# 評価パケット: T031–T033 — 整理のための自由タグ（付与・一括・視点切替）

ステータス: **保留**（起草 2026-07-25 planner / 2026-07-25 human「まだ実装しない。チケットのみでよい」）

GOは出ていない。再開するときは §7 のA〜Dに答えるところから。数字は再開時に実測し直すこと。

対象カード: [T031](../cards/T031-tag-attach-with-suggest.md) / [T032](../cards/T032-bulk-tag-and-consolidate.md) / [T033](../cards/T033-switch-thinking-by-tag.md)

### 0. 先に: 行数の数え方が過去パケットと揃っていない（要確認）

PLAN §11-3 は「アプリ本体(ui/ + serve.ts)は 15ファイル / 3,000行」と書くが、過去パケットの実数が
互いに再現できない（T024=2,986/11ファイル、T026=3,126/10ファイル、T025=3,632/13ファイル）。
ファイル数の数え方だけでも 10（TS/TSX のみ）と 13（`ui/` 全部 + `serve.ts`）が混在している。

**本パケットは2通りを併記し、判断は文字通りの定義（`ui/` 全部 + `serve.ts`）を主とする。**
数え方の統一は本パケットの対象外（必要なら別途）。

### 1. 状況（数字・2026-07-25 実測）

| 項目 | 値 |
| --- | --- |
| 現在 本体行数（`ui/` 全部 + `serve.ts`・**主**） | **4,385** / 3,000（超過 +1,385） |
| 現在 本体行数（TS/TSX のみ・参考） | 3,233 / 3,000（超過 +233） |
| 現在 ファイル数 | **13** / 15（`ui/` 12 + `serve.ts`） |
| 見込み純増（TS/TSX） | **+330〜360** |
| 見込み純増（`styles.css`） | +40〜60（選択表示・タグチップ・矩形） |
| GO後の着地見込み（主） | **約 4,755〜4,805 行**（超過 +1,755〜1,805） |
| GO後の着地見込み（TS/TSX） | 約 3,563〜3,593 行 |
| ファイル数の着地 | **13 または 14**（下記 §3 の判断次第） |
| テスト枠 | +60〜100（別枠 800・現状 tests 合計 1,273 で既に超過。unit のみ・smoke 本数は触らない） |

内訳（現在）: `board.tsx` 1,106 / `styles.css` 1,117 / `project.ts` 637 / `state.ts` 600 /
`main.tsx` 260 / `inspector.tsx` 242 / `stream.tsx` 126 / `db.ts` 89 / `types.ts` 79 /
`capture-notation.ts` 58 / `capture.tsx` 36 / `index.html` 14 / `serve.ts` 21

### 2. なぜ足すか（需要）

- 出典: 2026-07-25 人間との設計相談。T020案B（復活時の制約）の発動として起票
- 今困っている操作: 「これらのカードは○○でまとまるかも」を表す手段が**考察カード1枚＋糸n本しかない**。
  線でボードが重くなり、まだ主張になっていないハブが要素カードとして増えすぎる
- 設計の芯: **タグ＝描画されないハブカード**。考察カード＋糸と同型で、線を描かず・場所を取らず・
  名前1つで済む軽量表現。正本の位置は関係層のままなので §1「意味分類はタグにしない」と衝突しない
- Won't / 「やらない」との関係: T020で「自由入力タグはMVP外（案A）」と確定済み。**本パケットは
  その撤回ではなく案Bの発動**。案B4制約（主要導線はタグなしで完結／意味の正本にしない／
  ワークフロー状態にしない／削除・改名・統合できる）を実装で担保する
- **T033はPLAN改訂17を伴う**（T018「ビューの入力はタグではない」の読み直し）。GOは改訂17の採択を兼ねる

### 3. 入れるもの（変更の地図）

#### T031 — サイドから付ける（+150見込み）

| 塊 | ユーザーに見える変化 | 触る場所 | 見込み行 |
| --- | --- | --- | ---: |
| A. 付与・削除UI | カード選択中にサイドでタグを付け外しできる | `inspector.tsx` | +40〜55 |
| B. サジェスト | 既出タグが候補に出て↑↓Enterで選べる | `inspector.tsx` + 純関数 | +45〜60 |
| C. 種類数の抑止 | 新語は「新規作成」を明示的に選ぶ。12種で打ち止め | 純関数 + `inspector.tsx` | +30〜40 |
| D. 未定着表示 | 使用1件のタグが区別できる | `inspector.tsx` + `styles.css` | +15〜25 |

**ファイル数の判断**: B/C の純関数を `ui/tags.ts` として切り出すと**14ファイル**になる（警告基準15に接近）。
`capture-notation.ts` の前例どおり切り出せば unit テストが書けるが、
`state.ts`（600行）へ同居させれば13のまま。**planner推奨は切り出し**（テスト可能性を優先）。

#### T032 — 矩形選択で一括（+100〜130見込み）

| 塊 | ユーザーに見える変化 | 触る場所 | 見込み行 |
| --- | --- | --- | ---: |
| E. 矩形選択 | 空白ドラッグ（修飾つき）で複数カードを囲める | `board.tsx` `onCanvasPointerDown` + drag `type:"marquee"` | +40〜60 |
| F. 選択集合 state と表示 | 選択中のカードが目で分かる | `state.ts` + `styles.css` | +15 |
| G. 1枚ずつ足し引き | 紙部分クリックでトグル（**ノリ部分は従来のまま**） | `board.tsx` `onPaperPointerDown` | +5 |
| H. 一括付与UI | 選択集合へまとめてタグを付ける | `inspector.tsx` or ツールバー | +30 |
| I. 統合・改名 | タグをまとめる／名前を変えると全カードに反映 | 純関数 + UI | +30〜40 |

#### T033 — タグを起点に視点を切り替える（+80見込み）

| 塊 | ユーザーに見える変化 | 触る場所 | 見込み行 |
| --- | --- | --- | ---: |
| J. 焦点起点の型拡張 | （内部）起点がカードでもタグでもよくなる | `state.ts` `focusCardId` | +20 |
| K. 初期集合の分岐 | タグ持ちカードが1ホップ目になる | `board.tsx` `focusSet` | +10 |
| L. サイドのタグボタン | 検索の近くのタグをクリックで視点へ入る。−＋×もそこ | `main.tsx` or `stream.tsx` + `styles.css` | +40 |
| M. 契約同期（改訂17） | — | `PLAN.md` | +10 |

**流用できるもの（新規実装しない）**: ホップ展開 `expandFocusHops`/`shrinkFocusHops`、圏外を沈める描画、
「糸は両端が集合内のときだけ描く」、session限定の扱い — すべてT018の実装をそのまま使う。

### 4. 入れないもの / 落とすもの

- 今回やらない: キャプチャ行内記法（`#tag`）／Shift+クリックの複数選択／タグの色・階層・エイリアス／
  AND/OR複合条件／タグ絞り込み（沈めるのであって消さない）／タグ→考察カードの昇格変換／
  ピン・未読の専用操作
- 相殺で消すもの: **なし**（純増で超過を人間GOで通す案）
- 消す場合の体験への影響: 該当なし

### 5. 他の抑止機構チェック（照合・断定しない）

| チェック | 結果 | 根拠1行 |
| --- | --- | --- |
| 実プレイ起点か | **不明** | 2026-07-25の設計相談が出典。T020案Bは「痛みの観測後に再検討」と定めており、**具体的な痛みの記録がまだカードに無い**（§7で問う） |
| Won't に触れていないか | Yes | 複数ボード／Era／Entity辞書／版管理UIなし。タグ絞り込みも作らない |
| 新規依存を足さないか | Yes | 既存 Preact/Deno のみ |
| E2E/スモーク枠を増やさないか | Yes | unit のみ追加。smoke 本数は触らない |
| 静的配布を壊さないか | Yes | `tags` は schema v1 に既存。フォーカスは session のみ。Pages 配布形は不変 |
| 帳尻合わせ（一行化等）をしていないか | Yes | 相殺削除・圧縮なし |

### 6. リスクと確認方法

- 壊れうる既存操作:
  - **T014の付箋ノリドラッグ**（最大の注意点。ノリ部分の挙動は選択の有無にかかわらず不変であること）
  - 空白ドラッグのパン（矩形選択と取り合う）
  - **T018のカード起点フォーカスビュー**（起点の型を変えるため）
  - 検索（`filteredCards` は既に tags を見ている）、JSON往復
- GO後の人間確認（3ステップ以内）:
  1. カードを選びサイドからタグを付ける → 2つ目以降で候補が出る → 13種目が作れない
  2. 矩形で3枚囲んで1枚外し、まとめてタグを付ける → その間ノリを掴んだカードは普通に動く
  3. サイドのタグボタンを押す → その一族だけ浮かび、＋で広がり、×で戻る
- 見積もりが外れやすい点:
  - **矩形選択とパンの分岐**（`board.tsx` は既に1,106行で drag 状態機械が3種ある。ここが膨らみやすい）
  - 起点の型変更が `board.tsx` のバー表示条件へ波及する範囲
  - 統合・改名の取り消し（undo は無いので、誤操作時の体験を詰めると膨らむ）

### 7. ユーザーへの問い

**A. 予算**: 主定義で着地約4,800行（警告基準の1.6倍）を通すかどうか。

**B. 痛みの記録**（§5で「不明」とした点）: T020案Bは「痛みが観測され、専用操作より自由タグが
明らかに安いと分かってから再検討する」と定めた。今回の設計相談は**構造的な理由**（考察カード＋糸だと
線が重く要素カードが増えすぎる）としては筋が通っているが、**実プレイでの具体的な痛み**が
まだカードに記録されていない。あるなら1行、カード作業ログに残したい。無いまま通すなら
「構造的理由のみで案Bを発動する」という判断として記録する。

**C. 改訂17**: T033のGOはPLAN §1・§14の改訂（T018の起点をカード／タグへ広げる）の採択を兼ねる。

**D. ファイル数**: `ui/tags.ts` を切り出して14ファイルにするか（planner推奨・テスト可能性）、
`state.ts` へ同居させて13のままにするか。

次のいずれかで返答してください。

- **GO** — このまま実装してよい
- **条件付きGO** — 条件: …
- **NOGO** — 理由: …

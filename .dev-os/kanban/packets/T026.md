# 評価パケット: T026 — キャプチャの行内記法（ひとこと・出典URL）

ステータス: **GO**（2026-07-21 人間）

### 1. 状況（数字）

| 項目 | 値 |
| --- | --- |
| 現在 本体行数 / 警告基準 | 3,126 / 3,000 |
| 現在 ファイル数 / 警告基準 | 10 / 15 |
| 見込み純増（本体） | **+105〜155**（中央値 **+130**） |
| GO後の着地見込み | **約 3,230〜3,280 行**（基準超過 **+230〜280**） |
| テスト枠 | +50〜80（別枠 800・現在 765） |

### 2. なぜ足すか（需要）

- 出典: 2026-07-20 人間との設計相談で行内記法案を採用 / カード T026
- 今困っている操作: 3秒キャプチャでひとことと出典URLを残せない。`Card.url` はモデルにあるが入力も表示もない
- Won't / 「やらない」との関係: OGP自動取得は Won't。既出出典サジェストは T027。非URL出典の専用フィールドは今回やらない

### 3. 入れるもの（変更の地図）

| 塊 | ユーザーに見える変化 | 触る場所 | 見込み行 |
| --- | --- | --- | ---: |
| A. 行内記法パーサー | なし（純関数） | 新規 `ui/capture-notation.ts`（T027流用想定） | +45〜60 |
| B. キャプチャ入力 | `//` でひとこと、URL自動振り分け、placeholderヒント | `capture.tsx` + `state.ts`（`addCard` に body/url） | +20〜35 |
| C. 発見ログに出典 | カードから出典リンクを開ける | `stream.tsx` + `styles.css` | +20〜35 |
| D. インスペクタurl欄 | 出典を後から直せる（最小） | `inspector.tsx` + `state.ts`（`updateCard`）+ `types.ts`（`card_updated.url?`） | +20〜30 |

合計見込み: **+105〜155**（相殺削除なし・新規本体ファイル1）

記法（受け入れ確定案）:

- `タイトル // ひとこと` → title + body（1 Enter）
- 行内の `http(s)://…` → `url` へ格納し、タイトル文字列からは除く
- URLのみ → ホスト名を仮タイトル（後から改題可）
- 記法なしの従来1行入力は不変
- role は finding のまま（新role・印象種別なし）

### 4. 入れないもの / 落とすもの

- 今回やらない: 既出出典サジェスト（T027）、非URL出典フィールド、OGP、本格インスペクタ拡張
- 相殺で消すもの: **なし**（純増で警告超過を人間GOで通す案）
- 消す場合の体験への影響: 該当なし

### 5. 他の抑止機構チェック（照合・断定しない）

| チェック | 結果 | 根拠1行 |
| --- | --- | --- |
| 実プレイ起点か | Yes | 出典入力不能が原則乖離として起票・設計相談で記法採用 |
| Won't に触れていないか | Yes | OGPはWon't。サジェストはT027 |
| 新規依存を足さないか | Yes | 既存 Preact/Deno のみ |
| E2E/スモーク枠を増やさないか | Yes | unit のみ追加予定。smoke 本数は触らない |
| 静的配布を壊さないか | Yes | schema v1のまま任意フィールド利用。Pages 配布形は不変 |
| 帳尻合わせ（一行化等）をしていないか | Yes | 相殺削除・圧縮なし |

### 6. リスクと確認方法

- 壊れうる既存操作: プレーン1行キャプチャ、連続保存レース、考察側 `addCard`、エクスポート／インポート
- GO後の人間確認（3ステップ以内）:
  1. `題 // ひとこと https://example.com` をEnterし、title/body/urlが分かれて入る
  2. 発見ログから出典リンクが開き、インスペクタでurlを直せる
  3. 記法なしの1行キャプチャが今まで通り動く
- 見積もりが外れやすい点: stream で button 内にリンクを置けないため、行構造の直し方が膨らむ

### 7. ユーザーへの問い

次のいずれかで返答してください。

- **GO** — このまま実装してよい
- **条件付きGO** — 条件: …
- **NOGO** — 理由: …

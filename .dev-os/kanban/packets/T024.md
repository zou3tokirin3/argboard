# 評価パケット: T024 — 操作ログの記録だけ始める

ステータス: **GO**（2026-07-21 人間）

### 1. 状況（数字）

| 項目 | 値 |
| --- | --- |
| 現在 本体行数 / 警告基準 | 2,986 / 3,000 |
| 現在 ファイル数 / 警告基準 | 11 / 15 |
| 見込み純増（本体） | **+55〜75**（中央値 **+65**） |
| GO後の着地見込み | **約 3,041〜3,061 行**（基準超過 **+41〜61**） |
| テスト枠 | +30〜50（別枠 800・現在 218） |

### 2. なぜ足すか（需要）

- 出典: 2026-07-20 振り返り配信でグラフの育ちを追いたい観測 / カード T024
- 今困っている操作: 置いた・繋いだ・消した・要検討にした時刻が残らず、後から育ちを再生できない（表示は T025。記録は後付け不可）
- Won't / 「やらない」との関係: 再生UI・undo・件数上限は今回やらない（カード明記）

### 3. 入れるもの（変更の地図）

| 塊 | ユーザーに見える変化 | 触る場所 | 見込み行 |
| --- | --- | --- | ---: |
| A. events型 | なし（データ形だけ） | `types.ts`（`Project.events?` + イベントunion） | +18〜25 |
| B. 追記ヘルパ | なし | `project.ts`（`appendEvent` 数行） | +4〜8 |
| C. 確定操作で追記 | 体感は現状どおり。ログは裏で増える | `state.ts`（追加/編集/削除/糸/配置/開く） | +30〜40 |
| D. ドラッグ確定だけ記録 | ドラッグ中は今まで通り。離したとき1件 | `board.tsx` + `state.ts`（`flushSave`と分離） | +3〜8 |

合計見込み: **+55〜75**（交換なし・新規ファイルなし）

記録するイベント（確定時のみ）:

- `project_opened` / `card_added` / `card_updated` / `card_removed`（カード＋カスケード糸のスナップショット）
- `link_added` / `link_updated` / `link_removed`（削除は糸スナップショット）
- `card_placed`（初回配置・移動確定。中間座標は書かない）

### 4. 入れないもの / 落とすもの

- 今回やらない: 再生UI（T025）、undo/redo、ログ圧縮・件数上限、viewport/mode の記録、新規依存
- 相殺で消すもの: **なし**（純増で警告超過を人間GOで通す案）
- 消す場合の体験への影響: 該当なし

### 5. 他の抑止機構チェック（照合・断定しない）

| チェック | 結果 | 根拠1行 |
| --- | --- | --- |
| 実プレイ起点か | Yes | 振り返り配信で育ち再生の需要を観測して起票 |
| Won't に触れていないか | Yes | 再生UI・履歴UIは T025 側。本カードは記録のみ |
| 新規依存を足さないか | Yes | 既存 Preact/Deno のみ |
| E2E/スモーク枠を増やさないか | Yes | unit のみ追加予定。smoke 本数は触らない |
| 静的配布を壊さないか | Yes | schema v1のまま任意フィールド追加。Pages 配布形は不変 |
| 帳尻合わせ（一行化等）をしていないか | Yes | 相殺削除・圧縮フォーマットなし |

### 6. リスクと確認方法

- 壊れうる既存操作: キャプチャ連続保存、カード移動、エクスポート／インポート往復
- GO後の人間確認（3ステップ以内）:
  1. カードを置いて糸を張り、エクスポートJSONに `events` が増えている
  2. そのJSONをインポートし直し、`events` が残っている
  3. 画面上に履歴UIが出ていない
- 見積もりが外れやすい点: 削除スナップショットの持ち方で `state.ts` が膨らむ

### 7. ユーザーへの問い

次のいずれかで返答してください。

- **GO** — このまま実装してよい
- **条件付きGO** — 条件: …
- **NOGO** — 理由: …

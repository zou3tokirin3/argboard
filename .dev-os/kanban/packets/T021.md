# 評価パケット: T021 — 探索で出来事を上からも追加する

ステータス: **GO（案A）**（2026-07-28 人間: 現状充足で閉じる）

対象カード: [T021](../cards/T021-add-event-from-top.md)

発火理由: カードが「できたら」枠のまま backlog。**現状UIは探索キャプチャが既に最上段**なので、
「まだ足すのか／充足で閉じるのか」を人間に決めてもらう材料。予算ゲート単体では必須ではない。

### 1. 状況（数字）

計測: `deno task budget` / `deno task budget --since main`（定義正本: PLAN §4）
stock 計測日: 2026-07-28。Δ(`main`..) は作業ツリー現況（カード未着手）。

| 指標 | stock | 見込み Δ | しきい値 | 超過? |
| --- | ---: | ---: | ---: | --- |
| 本体行数 | 6,577 | **+15〜45**（中央 **+30**）※足す場合 | +200 | **No** |
| 操作の数 | 45 | **+0**（同一 `capture-input` 再利用想定） | +3 | No |
| 概念の数 | 92 | **+0** | +2 | No |
| 配布サイズ（gzip） | 38,319 B | **+0〜1 KB** | +8KB | No |
| Won't 抵触（§12） | — | — | Yes で発火 | **No** |
| スモーク本数 | 5 | +0 本の予定 | 上限5 | No |
| unit 行（申告） | 1,561 | +0 | 申告のみ | |

計器（止めない）: 本体ファイル数 **19**。Δ(`main`..) 本体行 −4 / gzip −12 B（本カード無関係）。

**判定**: flow 全指標しきい値未満 → 実装するとしてもパケット必須ではない。本パケットは**需要の有無とスコープ**のため。

### 2. なぜ足すか（需要）

- 出典: T015 / O3「出来事追加を上からもやりたい」、カード T021（できたら枠）
- 今困っている操作（起票時）: 探索で追加入力が下だけだと、新しい順リスト（上）と視線・手がずれてテンポが落ちる
- **現状コード（2026-07-28）**:
  - 探索: `main.tsx` で `<Capture />` がヘッダ直下・`Stream` の**上**（PLAN §探索「最上部にキャプチャ」どおり）
  - 考察サイド: `side-panel` 内でも Capture が Stream の上
  - 新規カードは `foundAt` 降順でリスト**先頭**に出る
- Won't / 「やらない」との関係: 表UI化・並び替えUI本格化はカードどおり外。タイムラインビュー（§12）にも触らない

### 3. 入れるもの（変更の地図）

**採用方針は GO 時に1つ選ぶ。** ここは候補の地図。

| 案 | ユーザーに見える変化 | 触る場所 | 見込み（行/操作/概念） |
| --- | --- | --- | ---: |
| **A. 充足確認で閉じる** | 変化なし。探索最上段キャプチャで O3 は満た済みとみなす | カード／PLAN のみ（実装なし） | 0 / 0 / 0 |
| **B. リスト先頭付近に第2入力** | 検索・フィルタの下／カード列の直上からも同じ1行キャプチャできる（既存最上段は残す） | `stream.tsx` + `capture.tsx`（props化で id 重複回避）+ `styles.css` | +25〜45 / 0 / 0 |
| **C. 探索だけ Stream 内へ移す** | キャプチャが発見ログ見出し直下に寄る（ページ最上段から一段下がる） | `main.tsx` + `stream.tsx` | +15〜30 / 0 / 0 |

推奨（実装するなら）: **B** — PLANの「最上部」を壊さず、「リスト近傍からも」を足す。  
ただし二重入力は hint/`aria-describedby`/`data-testid` の重複に注意（Capture を props 化する程度）。

合計見込み（案B）: 本体 **+25〜45** / 操作 **+0** / 概念 **+0** / gzip **+0〜1KB**

### 4. 入れないもの / 落とすもの

- 今回やらない: 表UI、並び替えUI、モバイル専用フローティングボタン、Cmd+K フォーカスの新規実装（PLAN記載あるが本カード外）、考察ボードの考察入力の複製
- 相殺で消すもの: **なし**（案Aなら増減ゼロ）
- 消す場合の体験への影響: 該当なし

### 5. 他の抑止機構チェック（照合・断定しない）

| チェック | 結果 | 根拠1行 |
| --- | --- | --- |
| 実プレイ起点か | Yes | T008ドッグフード観測 O3 |
| Won't に触れていないか | Yes | 表／並び替え／タイムラインビューに手を出さない |
| 新規依存を足さないか | Yes | 既存 Capture の配置変更のみ |
| スモーク本数を増やさないか | Yes | 既存 capture 導線の回帰確認のみ |
| 静的配布を壊さないか | Yes | クライアントUIのみ・スキーマ不変 |
| 帳尻合わせ（一行化等）をしていないか | Yes | 見積もりは素直な小幅純増 or ゼロ |

### 6. リスクと確認方法

- 壊れうる既存操作: 探索キャプチャ（`//`・URL記法含む）、連続キャプチャ保存（T007）、考察サイドのキャプチャ、`capture-input` を使う smoke
- GO後の人間確認（3ステップ以内）:
  1. 探索で最上段（と、案Bならリスト直上）から1行追加 → 先頭に出る
  2. 連続で数件入れて欠けない
  3. 考察サイドのキャプチャも従来どおり動く
- 見積もりが外れやすい点: Capture の二重マウントで id / testid 衝突を直すと +10 乗りやすい。案Cは PLAN文言とのズレ確認が要る

### 7. ユーザーへの問い

次のいずれかで返答してください。

- **GO（案A）** — 現状の最上段キャプチャで O3 充足。T021 は実装せず done / dropped へ（列遷移は人間）
- **GO（案B）** — リスト先頭付近に第2キャプチャを足してよい（見込み +25〜45）
- **GO（案C）** — 探索キャプチャを Stream 内へ移してよい
- **条件付きGO** — 条件: …
- **NOGO** — 今はやらない（できたら枠のまま backlog 維持）。理由: …

---

**採択 2026-07-28 human: GO（案A）** — 追加実装なし。T021 → done。

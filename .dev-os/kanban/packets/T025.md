# 評価パケット: T025 — グラフの育ちを時系列で再生する

ステータス: **GO**（2026-07-25 人間）

### 1. 状況（数字）

| 項目 | 値 |
| --- | --- |
| 現在 本体行数 / 警告基準 | 3,632 / 3,000 |
| 現在 ファイル数 / 警告基準 | 13 / 15（`ui/` 12 + `serve.ts`） |
| 見込み純増（本体） | **+155〜240**（中央値 **+195**） |
| GO後の着地見込み | **約 3,787〜3,872 行**（基準超過 **+787〜872**） |
| テスト枠 | +50〜90（別枠 800・現状 tests 合計は既に超過。unit のみ追加・smoke 本数は触らない） |

### 2. なぜ足すか（需要）

- 出典: 2026-07-20 振り返り配信でグラフの育ちを追いたい観測 / カード T025（T024記録済み）
- 今困っている操作: 考察ボードは「いま」しか見えず、カードと糸がどう増えたかをスライダーで追い回せない
- Won't / 「やらない」との関係:
  - §12「タイムラインビュー」= 出来事の時系列**一覧**ビュー。本カードはボード状態の**時点再生**であり別物
  - 実装時に PLAN §12 へ一文注記する（一覧は作らない／再生は T025）
  - 配信演出（チャプター・幽霊・プレゼン・再生アニメ）はカード Won't のまま

### 3. 入れるもの（変更の地図）

| 塊 | ユーザーに見える変化 | 触る場所 | 見込み行 |
| --- | --- | --- | ---: |
| A. 時点ビュー純関数 | なし（表示の材料） | `project.ts`（`viewAt`） | +55〜85 |
| B. session 再生位置 | リロードで解け、JSONには載せない | `state.ts`（`replayAt` + 派生ビュー + 書込ガード） | +30〜50 |
| C. 再生UI | 考察モードでスライダー／時刻表示／「いまに戻る」 | `board.tsx` ツールバー | +40〜65 |
| D. 読み取り専用表示 | 再生中は置かない・繋がない・消さない・編集しない | `board.tsx` + `inspector.tsx` + 考察追加フォーム | +20〜30 |
| E. 見た目 | 再生バーと「再生中」が分かる最小スタイル | `styles.css` | +15〜25 |
| F. 契約注記 | Won't「タイムラインビュー」と再生の境界を一文 | `PLAN.md` §12 | +5〜10 |

合計見込み: **+155〜240**（新規本体ファイルなし・相殺削除なし）

再生の契約（受け入れ確定案）:

- **段階1（events が薄い／無い期間）**: `foundAt` / `createdAt` でその時点までに存在したカードと糸だけを出す。位置は現在位置。削除済みは出ない
- **段階2（events がある期間）**: `events` を `at` まで再生し、配置・削除・要検討（糸 kind）切替を反映。位置は直近の `card_placed`
- 再生位置は **session のみ**（`Project` / エクスポートに書かない）
- 再生中は読み取り専用。探索モードへ切替または「いまに戻る」で通常表示へ戻る
- 焦点ビュー（T018）は再生中も、見えている集合の上で動いてよい（元データは触らない）

### 4. 入れないもの / 落とすもの

- 今回やらない: チャプター送り、削除物の幽霊表示、プレゼンモード、自動再生アニメ、動画/GIF書き出し、undo/redo、履歴イベント一覧UI、viewport/mode の再生
- 相殺で消すもの: **なし**（純増で警告超過を人間GOで通す案）
- 消す場合の体験への影響: 該当なし

### 5. 他の抑止機構チェック（照合・断定しない）

| チェック | 結果 | 根拠1行 |
| --- | --- | --- |
| 実プレイ起点か | Yes | 振り返り配信で育ち再生の需要を観測して起票。T024で記録土台済み |
| Won't に触れていないか | Yes | 一覧タイムラインは作らない。再生と境界を §12 に注記。配信演出は対象外 |
| 新規依存を足さないか | Yes | 既存 Preact/Deno のみ（`<input type="range">`） |
| E2E/スモーク枠を増やさないか | Yes | unit のみ追加予定。smoke 本数は触らない |
| 静的配布を壊さないか | Yes | schema 不変・session のみ。Pages 配布形は不変 |
| 帳尻合わせ（一行化等）をしていないか | Yes | 相殺削除・圧縮なし |

### 6. リスクと確認方法

- 壊れうる既存操作: カード移動・糸張り・考察追加・インスペクタ編集・焦点ビュー・エクスポート
- GO後の人間確認（3ステップ以内）:
  1. 考察モードでスライダーを戻す → その時点より後のカード／糸が消える
  2. 「いまに戻る」または探索へ切替 → 全件が戻り、編集できる
  3. 再生中にドラッグや考察追加を試す → 書き込まれない
- 見積もりが外れやすい点: `viewAt` の段階1/2合流と、再生中ガードが `board.tsx` で想定より膨らむ

### 7. ユーザーへの問い

次のいずれかで返答してください。

- **GO** — このまま実装してよい
- **条件付きGO** — 条件: …
- **NOGO** — 理由: …

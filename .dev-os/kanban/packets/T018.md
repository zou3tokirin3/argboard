# 評価パケット: T018 — 関係ベースのフォーカスビュー

ステータス: **GO待ち**（2026-07-24 起票。旧 `T018-T020.md` は改訂13で失効済み・本パケットが正）

### 1. 状況（数字）

| 項目 | 値 |
| --- | --- |
| 現在 本体行数 / 警告基準 | 3,367 / 3,000 |
| 現在 ファイル数 / 警告基準 | 13 / 15（`ui/` 12 + `serve.ts`） |
| 見込み純増（本体） | **+120〜180**（中央値 **+150**） |
| GO後の着地見込み | **約 3,487〜3,547 行**（基準超過 **+487〜547**） |
| テスト枠 | +40〜70（別枠 800・現状 tests 合計は既に超過。unit のみ追加・smoke 本数は触らない） |

### 2. なぜ足すか（需要）

- 出典: T015 / O5 → T018（改訂13でタグ非依存へ再定義）。T028/T029/T020 探索 done 済み
- 今困っている操作: 1枚のボードのまま「このカードに関係するものだけ見たい」に切り替えられない。関連強調（T017/T023）はあるが、圏外を沈めて視点として固定する導線がない
- Won't / 「やらない」との関係: 複数ボード・Era／タイムライン専用ビュー・タグ絞り込み・作中時系列は作らない（カード明記・改訂13/15）

### 3. 入れるもの（変更の地図）

| 塊 | ユーザーに見える変化 | 触る場所 | 見込み行 |
| --- | --- | --- | ---: |
| A. session 焦点状態 | リロードで解け、JSONには載せない | `state.ts`（`focusCardId` / `focusHops`） | +15〜25 |
| B. Nホップ到達 | 起点から糸で届く集合（thought含む） | `board.tsx` または小さな純関数 | +20〜35 |
| C. 沈める表示 | 圏外カードは薄く残る（消さない） | `board.tsx` + `styles.css` | +25〜40 |
| D. 操作UI | 「この視点で見る」／「もう一周」／「全部見る」 | `board.tsx` ツールバー | +30〜45 |
| E. 触れない圏外 | 沈んだカードはドラッグ・糸・選択不可 | `board.tsx` hit/pointer | +15〜25 |
| F. 糸の描画 | 両端とも焦点集合内のときだけ描く | `board.tsx` | +10〜15 |
| G. 契約同期 | 焦点ビューと糸の扱いを一文でPLANへ | `PLAN.md` | +10〜15 |

合計見込み: **+120〜180**（新規本体ファイルなし・タグUIなし）

仕様メモ（カードの暫定判断を実装契約に落とす）:

- 選択の既定挙動は変えない（青／関連強調はそのまま）。焦点は**別操作**「この視点で見る」
- 既定ホップ=1。「もう一周」で +1。スライダーは作らない
- 「全部見る」で session 焦点を解除（蝶番）
- 元データ（positions / cards / links）は複製・変更しない
- 入力は糸のみ（タグ・意味分類は使わない）

### 4. 入れないもの / 落とすもの

- 今回やらない: タグ付け／タグ絞り込み、名前付きビューの棚、昇格A（糸→考察）、作中日時・タイムライン、ホップ・スライダー、沈んだカードへのクリック乗り換え、複数ボード
- 相殺で消すもの: **なし**（純増で警告超過を人間GOで通す案）
- 消す場合の体験への影響: 該当なし

### 5. 他の抑止機構チェック（照合・断定しない）

| チェック | 結果 | 根拠1行 |
| --- | --- | --- |
| 実プレイ起点か | Yes | T008→T015/O5。設計は改訂13＋T028/T029で閉じ済み |
| Won't に触れていないか | Yes | 複数ボード／Era／版管理ビュー／Entity辞書なし |
| 新規依存を足さないか | Yes | 既存 Preact/Deno のみ |
| E2E/スモーク枠を増やさないか | Yes | unit のみ追加予定。smoke 本数は触らない |
| 静的配布を壊さないか | Yes | schema 不変・session のみ。Pages 配布形は不変 |
| 帳尻合わせ（一行化等）をしていないか | Yes | 相殺削除・圧縮なし |

### 6. リスクと確認方法

- 壊れうる既存操作: カード選択・移動、糸張り／選択、関連強調、考察カード追加、サイド発見ログ
- GO後の人間確認（3ステップ以内）:
  1. 考察モードでカードを選び「この視点で見る」→ 関係先だけが濃く、他が沈む
  2. 「もう一周」で到達が広がり、「全部見る」で元に戻る
  3. 焦点中も浮いているカードの移動・糸張りができ、沈んだカードには触れない
- 見積もりが外れやすい点: hit／ゴムバンド／関連強調と焦点集合の分岐が `board.tsx` で想定より膨らむ

### 7. ユーザーへの問い

次のいずれかで返答してください。

- **GO** — このまま実装してよい
- **条件付きGO** — 条件: …
- **NOGO** — 理由: …
